{
  "name": "money2time-workers-receipt-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for money2time receipt scanning (Gemini 2.5 Flash Lite via OpenRouter).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --tsconfig tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260711.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.110.0"
  }
}
